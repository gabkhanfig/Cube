#pragma once

#include <Engine/Platform/PlatformTypes.h>

/* Signed 8 bit integer. */
typedef PLATFORM_TYPES::int8 int8;

/* Signed 16 bit integer. */
typedef PLATFORM_TYPES::int16 int16;

/* Signed 64 bit integer. */
typedef PLATFORM_TYPES::int64 int64;

/* Unsigned 8 bit integer. Technically a char so watch string conversion. */
typedef PLATFORM_TYPES::uint8 uint8;

/* Unsigned 16 bit integer. */
typedef PLATFORM_TYPES::uint16 uint16;

/* Unsigned 32 bit integer. */
typedef PLATFORM_TYPES::uint32 uint32;

/* Unsigned 64 bit ingteger. */
typedef PLATFORM_TYPES::uint64 uint64;